import "GPT.idsl";
import "StoryTelling.idsl";
import "GestorSG.idsl";
import "EboASR.idsl";
import "Speech.idsl";

Component storytelling
{
    Communications
    {
        implements StoryTelling;
        requires GPT, GestorSG, EboASR, Speech;
    };
    language python;
    gui Qt(QWidget);
    options dsr;
};

